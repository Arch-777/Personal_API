services:
  # Only started when DATABASE_URL is not set in .env
  # Usage: docker compose --profile local-db up
  db:
    profiles: [local-db]
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: localpass
      POSTGRES_DB: personalapi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command: ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-google:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=connector.google",
        "--hostname=worker-google@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-github:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=connector.github",
        "--hostname=worker-github@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-notion:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=connector.notion",
        "--hostname=worker-notion@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-spotify:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=connector.spotify",
        "--hostname=worker-spotify@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-slack:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=connector.slack",
        "--hostname=worker-slack@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-file-watcher:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=pipeline.file-watcher",
        "--hostname=worker-file-watcher@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-embedding:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--queues=pipeline.embedding",
        "--hostname=worker-embedding@%h",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

  worker-beat:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:localpass@db:5432/personalapi}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-prefer}
      REDIS_URL: redis://redis:6379/0
    command:
      [
        "celery",
        "-A",
        "workers.celery_app:celery_app",
        "beat",
        "--loglevel=INFO",
      ]
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./:/app
    restart: unless-stopped

volumes:
  redisdata:
  pgdata:
